<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Input UI" Height="200" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- TextBox for user input -->
        <TextBox x:Name="textBox" Margin="10" Grid.Row="0" Width="460" Height="40" FontSize="15" />

        <!-- Button to process the input -->
        <Button x:Name="addTextBtn" Click="addTextBtn_Click" Grid.Row="1" Content="Submit" Width="100" Height="40" Margin="10"/>
    </Grid>
</Window>